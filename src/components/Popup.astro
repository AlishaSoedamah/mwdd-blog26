---
---

<ol>
	<li>Do you want to install malware on your computer? (A high contrast theme)</li>
	<li>
		<ul>
			<li>
				<button>yes</button>
			</li>
			<li>
				<button>maybe later</button>
			</li>
		</ul>
	</li>
</ol>

<style>
	ol:first-of-type {
		color: var(--off-black);
		padding: 10px;
		margin: 0 auto;
		list-style-type: none;
		border: var(--off-black) 1px solid;
		background-color: var(--bg-content);
		width: 400px;
		li {
			button {
				color: var(--off-black);
				background: var(--bg-content);
				border: var(--off-black) 1px solid;
				padding: 10px;
				&:hover {
					cursor: pointer;
					background-color: var(--bg-hover);
				}
			}
			ul {
				padding: 0;
				list-style-type: none;
				li {
					display: inline;
				}
			}
		}
	}

</style>